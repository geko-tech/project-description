# Downloading plugins

@Metadata {
    @PageKind(article)
}

### Downloading plugins

Plugins are downloaded when you run: 
```bash
geko fetch
```

There's also a flag that allows you to download only plugins: 
```bash
geko fetch --plugins-only
```

Plugins are downloaded to your project folder **your_project/.geko/Plugins/PluginName**.
