name: pr

on:
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: [self-hosted, volkodav]
    steps:
      - uses: actions/checkout@v6.0.1

      - name: Build
        run: swift build